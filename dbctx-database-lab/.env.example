# ---------------------------------------------------------------------------
# dbctx Database Lab environment variables.
# Copy this file to .env and adjust values as needed:
#   cp .env.example .env
# ---------------------------------------------------------------------------

# ---------------------------------------------------------------------------
# Common database settings
# ---------------------------------------------------------------------------
DB_TIMEZONE=America/New_York
DB_DATABASE=app
DB_USERNAME=app
DB_PASSWORD=secret
DB_ROOT_PASSWORD=secret

# ---------------------------------------------------------------------------
# SQL Server SA password (must meet complexity requirements)
# ---------------------------------------------------------------------------
MSSQL_SA_PASSWORD=Str0ngP@ssw0rd!

# ---------------------------------------------------------------------------
# Host ports exposed on localhost
# ---------------------------------------------------------------------------
POSTGRES_PORT=5432
MARIADB_PORT=3306
MYSQL_PORT=3307
MSSQL_PORT=1433
ADMINER_PORT=8080
PGADMIN_PORT=5050
PHPMYADMIN_PORT=8081

# ---------------------------------------------------------------------------
# Resource limits for database services
# Values are passed directly to Docker Compose deploy.resources.
# ---------------------------------------------------------------------------
DB_CPU_LIMIT=1.0
DB_CPU_RESERVATION=0.25
DB_MEMORY_LIMIT=1G
DB_MEMORY_RESERVATION=256M

# SQL Server requires at least 2 GB of memory.
MSSQL_CPU_LIMIT=2.0
MSSQL_CPU_RESERVATION=1.0
MSSQL_MEMORY_LIMIT=2G
MSSQL_MEMORY_RESERVATION=1G

# ---------------------------------------------------------------------------
# PostgreSQL tuning
# ---------------------------------------------------------------------------
POSTGRES_SHARED_BUFFERS=128MB
POSTGRES_WORK_MEM=4MB
POSTGRES_MAINTENANCE_WORK_MEM=64MB
POSTGRES_MAX_CONNECTIONS=200

# ---------------------------------------------------------------------------
# MySQL tuning
# ---------------------------------------------------------------------------
MYSQL_INNODB_BUFFER_POOL_SIZE=128M

# ---------------------------------------------------------------------------
# MariaDB tuning
# ---------------------------------------------------------------------------
MARIADB_INNODB_PAGE_SIZE=16384

# ---------------------------------------------------------------------------
# TLS / SSL
# Set ENABLE_TLS=true to generate development certificates and require TLS on
# engines that support it. SQL Server, MariaDB, and MySQL can additionally
# require TLS with *_REQUIRE_TLS variables.
# ---------------------------------------------------------------------------
ENABLE_TLS=false
TLS_CN=dbctx.local
TLS_DAYS=365

# When set to ON/1/true, clients must connect over TLS.
MARIADB_REQUIRE_TLS=OFF
MYSQL_REQUIRE_TLS=OFF

# ---------------------------------------------------------------------------
# pgAdmin credentials
# ---------------------------------------------------------------------------
PGADMIN_EMAIL=admin@dbctx.local
PGADMIN_PASSWORD=secret

# ---------------------------------------------------------------------------
# Logging
# ---------------------------------------------------------------------------
LOG_MAX_SIZE=10m
LOG_MAX_FILE=3
