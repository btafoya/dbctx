# dbctx Database Lab

This directory is a standalone compatibility laboratory for `dbctx`. It is
not part of the core `dbctx` product; the main project continues to support
only MySQL, MariaDB, and SQL Server per `VISION.md`.

Use `cp .env.example .env`, then `make up`.
